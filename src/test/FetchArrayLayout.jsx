// import React from 'react';
// import FetchArrayPoke from './FetchArrayPoke';

// export default function FetchArrayLayout({ arrayPokes }) {
//   return (
//     <div style={{color : "red"}}>
//       {arrayPokes.map((poke) => (
//         <FetchArrayPoke />
      
//       //<div>{JSON.stringify(poke)}</div>))}
//     </div>
//   );
// }
//{arrayPokes.map(poke => (<div>{JSON.stringify(poke)}</div>))}